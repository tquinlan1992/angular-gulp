dependencies:
  pre:
        - npm install
    post:
        - npm run webdriver:
            background: true
        - npm run build-client
        - npm run nodemon-client-server &
        - npm run nodemon-api-server &
test:
  override:
    - npm run ui-tests
machine:
  node:
    version: 6.9.1
