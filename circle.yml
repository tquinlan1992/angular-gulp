machine:
  node:
      version: 6.9.1
  services:
      - docker

dependencies:
  override:
      - docker info
      - npm install

test:
  override:
      - npm run complexity-report
      - npm run build-client
      - npm run karma-tests
