dependencies:
  pre:
        - npm install
        - npm run build-client
        - npm run nodemon-client-server &
        - npm run nodemon-api-server &
test:
  override:
    - npm run ui-tests
machine:
  node:
    version: 6.9.1
